namespace ATMChallenge.Application
{
    /// <summary>
    /// Clase marcador usada para el escaneo de ensamblados (MediatR, validadores, etc.).
    /// No contiene lógica; su único propósito es permitir RegisterServicesFromAssemblies(typeof(ApplicationMarker).Assembly).
    /// </summary>
    public class ApplicationMarker
    {
    }
}
